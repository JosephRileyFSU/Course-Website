<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MAUI.project.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MAUI.project"
    xmlns:local2="clr-namespace:MAUI.project.Views"
    Shell.FlyoutBehavior="Disabled">

    <ShellContent
        Title="Login Screen"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
    <ShellContent
        Title="Instructor"
        ContentTemplate="{DataTemplate local2:InstructorView}"
        Route="InstructorView" />
    <ShellContent
        Title="Student"
        ContentTemplate="{DataTemplate local2:StudentView}"
        Route="StudentView" />
    <ShellContent
        Title="Create a new person"
        ContentTemplate="{DataTemplate local2:CreateNewPerson}"
        Route="CreateNewPerson" />
    <ShellContent
        Title="Course Details"
        ContentTemplate="{DataTemplate local2:CourseDetailView}"
        Route="CourseDetailView" />

</Shell>
